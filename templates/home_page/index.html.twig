{% extends 'base.html.twig' %}
{% block body %}
    {% for flashSuccess in app.flashes('success') %}
        <div class="alert alert-success" role="alert">{{ flashSuccess }}</div>
    {% endfor %}

        <div class="jumbotron">
         <h1>“A room without books is like a body without a soul.”</h1>
            <p> <small><b> Marcus Tullius Cicero</b></small></p>
        </div>
       <div class="btn btn-lg" style="margin-left: 5%">
        <span class=" glyphicon glyphicon-search"></span><a href="{{ path('search') }}">Search for your book </a>
       </div>


{% endblock %}
